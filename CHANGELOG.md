2024-07-29 | server.mjs | Implemented the final required server-side changes. Added the `PUT /api/v3/als/:id` endpoint to allow for inline editing and saving of AL data. The `/api/v3/export` endpoint now accepts a `format` parameter to handle both PDF and Excel exports. The project is now fully functional according to the v4 requirements. | All major fixes are complete. The application should be tested from end to end.